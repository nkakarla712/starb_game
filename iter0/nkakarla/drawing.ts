import { Canvas, createCanvas, addImageFromFile, getImage } from './image-library';

/**
 * Given a puzzle ADT, create the drawing for the puzzle
 * @returns Canvas object that represents the puzzle and partitions 
 *          in the puzzle object. Should be DIMSxDIMS size
 */
function drawPuzzle(puzzle:Puzzle): Canvas{
    
}